//MIX INS
/*
In other languages you can inherit one class and implements multiple ClassDecorator
In javascript use mix-ins
*/


//ARRAYS
/*
arrays can have holes. 

slice() copies an array (shallow copy)

slice(n) copies n through end of an array

slice(m, n) copies m through n-1

avoid for in statements

ITERATE THROUGH ARRAY
use array.forEach(function (x){

})
OR
array.forEach(x => console.log(x))

*/

//DEEP VS SHALLOW COPY
/*
Deep makes a new copy in memory
*/

//REGULAR EXPRESSION
/*
g = global; math multiple times
i = insensitive; ignore character case

str.replace(regex, "replacement string")
search - returns the index of  character the regex starts
exec - returns data on a sting
test - returns true or false
*/
